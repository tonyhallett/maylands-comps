import { CircularProgress } from "@mui/material";
import Centered from "../layoutComponents/Centered";

export default function CenteredCircularProgress() {
  return (
    <Centered>
      <CircularProgress />
    </Centered>
  );
}
